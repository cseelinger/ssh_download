# ------------------------------------------------------------
# Example .env for download_unzip_deleteFhir.sh (public-safe)
# ------------------------------------------------------------

# --- FHIR server (must end with /fhir)
FHIR_BASE="https://example.org/fhir"

# Prefix used to parse DocumentReference.masterIdentifier.value
# Default in script: NCT-DKFZ-DE_
SEARCH_PREFIX="NCT-DKFZ-DE_"

# --- Output directories (public-safe demo paths)
OUTDIR="./out"
OUTDIR_NAME="./out_name"

# --- Internal working dirs (relative to script directory by default)
RUN_DIR="./run"
LOG_DIR="./logs"
TMP_DIR="./tmp"
LOG="./logs/download_unzip_deleteFhir.log"

# Keep only log lines newer than X hours (default: 72)
LOG_RETENTION_HOURS="72"

# --- Networking / download parameters
CONNECT_TIMEOUT="5"
MAX_TIME="120"
RETRIES="2"
RETRY_DELAY="2"

# Pagination for DocumentReference listing
MAX_PAGES="50"
PAGE_SIZE="200"

# --- Deletion behavior (SAFETY: keep off for demo/public repos)
# 0 = do NOT delete DocumentReference/Binary after processing
# 1 = delete after successful processing
DELETE_AFTER_DOWNLOAD="0"

# Try DELETE .../_history even if not advertised (only relevant when deleting)
FORCE_HISTORY_DELETE="1"

# --- FileState delete logic
# If enabled and ZIP contains only FileState JSON, delete local files not listed there.
FILESTATE_DELETE_ENABLED="1"
FILESTATE_JSON_PATTERN="filestate.json"
